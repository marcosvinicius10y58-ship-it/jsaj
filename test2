local StarterGui = game:GetService("StarterGui")
local Players = game:GetService("Players")

-- ************************ Remove verificação de whitelist, entra direto ************************
local LocalPlayer = Players.LocalPlayer
while not LocalPlayer do
    Players.PlayerAdded:Wait()
    LocalPlayer = Players.LocalPlayer
end

-- Notificação de boas-vindas
pcall(function()
    StarterGui:SetCore("SendNotification", {
        Title = "Script Troll",
        Text = string.format("Bem-vindo %s, carregando script...", LocalPlayer.Name),
        Duration = 5,
    })
end)

warn("Script iniciado, carregando funções")

-- ************************ Funções principais ************************

-- Carregar WindUI
local success, WindUI = pcall(function()
    return loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
end)

if not success or not WindUI then
    game:GetService("StarterGui"):SetCore("SendNotification",{
        Title = "Erro",
        Text = "Falha ao carregar UI",
        Duration = 5;
    })
    return
end

-- Tema
WindUI:SetTheme("Midnight")

WindUI:Notify({
    Title = "Script Troll",
    Content = "Carregando...",
    Duration = 5,
})

task.wait(1)

-- Criar janela
local Window = WindUI:CreateWindow({
    Title = "Central Troll",
    Icon = "rbxassetid://6031280882",
    Author = "_xingchen1",
    Folder = "WindUI",
    Size = UDim2.fromOffset(580, 460),
    Transparent = false,
    Theme = "Light",
    ScrollBarEnabled = true,
})

Window:SetToggleKey(Enum.KeyCode.G)

-- Abas
local noticeSection = Window:Section({ Title = "Avisos", Opened = true })
local functionSection = Window:Section({ Title = "Funções", Opened = true })
local configSection = Window:Section({ Title = "Configurações", Opened = true })

Tabs.NoticeTab = noticeSection:Tab({ Title = "Avisos", Icon = "info" })
Tabs.GeneralTab = functionSection:Tab({ Title = "Principal", Icon = "settings" })
Tabs.AttackTab = functionSection:Tab({ Title = "Ataque", Icon = "sword" })
Tabs.ESPTab = functionSection:Tab({ Title = "ESP", Icon = "eye" })
Tabs.TeleportTab = functionSection:Tab({ Title = "Teleport", Icon = "map-pin" })

-- Avisos
Tabs.NoticeTab:Paragraph({
    Title = "Central Troll",
    Desc = "kkk",
    Image = "info",
    Color = "Red"
})

Tabs.NoticeTab:Paragraph({
    Title = "Atalho",
    Desc = "G: abrir/fechar menu",
    Image = "keyboard",
    Color = "Green"
})

-- Funções
Tabs.GeneralTab:Slider({
    Title = "Zoom da câmera",
    Value = { Min = 128, Max = 1000000, Default = 128 },
    Callback = function(value)
        LocalPlayer.CameraMaxZoomDistance = value
    end
})

Tabs.GeneralTab:Input({
    Title = "Velocidade",
    Value = "200",
    Placeholder = "Digite a velocidade"
})

Tabs.GeneralTab:Toggle({
    Title = "Ativar speed",
    Value = false
})

Tabs.GeneralTab:Button({
    Title = "Voar (atravessar paredes)"
})

Tabs.GeneralTab:Button({
    Title = "Remover lava"
})

Tabs.GeneralTab:Button({
    Title = "Remover neblina"
})

Tabs.GeneralTab:Button({
    Title = "Diminuir gráficos"
})

-- Ataque
Tabs.AttackTab:Toggle({
    Title = "Ativar ataque"
})

Tabs.AttackTab:Input({
    Title = "Distância do ataque"
})

Tabs.AttackTab:Input({
    Title = "Velocidade de ataque"
})

Tabs.AttackTab:Toggle({
    Title = "Atacar monstros"
})

Tabs.AttackTab:Toggle({
    Title = "Atacar jogadores"
})

Tabs.AttackTab:Toggle({
    Title = "Auto V4"
})

Tabs.AttackTab:Toggle({
    Title = "Auto V3"
})

Tabs.AttackTab:Toggle({
    Title = "Haki automático"
})