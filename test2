local StarterGui = game:GetService("StarterGui")
local Players = game:GetService("Players")

-- ************************ Remover verificação de whitelist, entrar direto ************************
local LocalPlayer = Players.LocalPlayer
while not LocalPlayer do
    Players.PlayerAdded:Wait()
    LocalPlayer = Players.LocalPlayer
end

-- Enviar notificação de boas-vindas
pcall(function()
    StarterGui:SetCore("SendNotification", {
        Title = "Script Troll",
        Text = string.format("Bem-vindo %s, carregando script...", LocalPlayer.Name),
        Duration = 5,
    })
end)

warn("Script iniciado, começando a carregar funções")

-- ************************ Abaixo estão as funções principais do script (mantidas completas) ************************

-- Carregar WindUI
local success, WindUI = pcall(function()
    return loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
end)

if not success or not WindUI then
    game:GetService("StarterGui"):SetCore("SendNotification",{
        Title = "Erro",
        Text = "Falha ao carregar UI",
        Duration = 5;
    })
    return
end

-- Definir tema
WindUI:SetTheme("Midnight")

-- Notificação básica
WindUI:Notify({
    Title = "Script Troll",
    Content = "Carregando",
    Duration = 5,
})

task.wait(1)

-- Inicializar serviços
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- Variáveis globais
_G.ToggleStates = _G.ToggleStates or {}
local translateSpeed = 50
local translateConnection = nil
local translateAccelEnabled = false
local _G = _G or getfenv(0)._G
_G.FastAttack = _G.FastAttack ~= nil and _G.FastAttack or true

-- Criar janela principal
local Window = WindUI:CreateWindow({
    Title = "Central Troll",
    Icon = "rbxassetid://6031280882",
    Author = "_xingchen1",
    Folder = "WindUI",
    Size = UDim2.fromOffset(580, 460),
    Transparent = false,
    Theme = "Light",
    ScrollBarEnabled = true,
})

-- Tecla G para abrir/fechar UI
Window:SetToggleKey(Enum.KeyCode.G)

-- Criar todas as abas
local Tabs = {}

-- Criar seções
local noticeSection = Window:Section({ Title = "Avisos", Opened = true })
local functionSection = Window:Section({ Title = "Funções principais", Opened = true })
local configSection = Window:Section({ Title = "Configuração de tema", Opened = true })

Tabs.NoticeTab = noticeSection:Tab({ Title = "Avisos", Icon = "info" })
Tabs.GeneralTab = functionSection:Tab({ Title = "Função 1", Icon = "settings" })
Tabs.AttackTab = functionSection:Tab({ Title = "Ataque em área", Icon = "sword" })
Tabs.ESPTab = functionSection:Tab({ Title = "ESP", Icon = "eye" })
Tabs.TeleportTab = functionSection:Tab({ Title = "Teleporte", Icon = "map-pin" })
Tabs.ActivityTab = functionSection:Tab({ Title = "Função 2", Icon = "zap" })
Tabs.texiaoTab = configSection:Tab({ Title = "Alterar efeitos", Icon = "palette" })
Tabs.ThemeTab = configSection:Tab({ Title = "Cores do tema", Icon = "palette" })

-- ===================== Aba de avisos =====================
Tabs.NoticeTab:Paragraph({
    Title = "Central Troll",
    Desc = "kkk",
    Image = "info",
    Color = "Red"
})

Tabs.NoticeTab:Divider()

Tabs.NoticeTab:Paragraph({
    Title = "Atalho",
    Desc = "G: abrir/fechar menu",
    Image = "keyboard",
    Color = "Green"
})

-- ===================== Aba geral =====================
local currentZoom = 128
Tabs.GeneralTab:Slider({
    Title = "Zoom da câmera",
    Value = { Min = 128, Max = 1000000, Default = 128 },
    Callback = function(value)
        currentZoom = value
        LocalPlayer.CameraMaxZoomDistance = value
        WindUI:Notify({ Title = "Zoom", Content = "Definido para: " .. value, Duration = 2 })
    end
})

Tabs.GeneralTab:Divider()

Tabs.GeneralTab:Input({
    Title = "Velocidade",
    Value = "200",
    Placeholder = "Digite o valor da velocidade",
    Callback = function(value)
        local speed = tonumber(value)
        if speed then
            translateSpeed = speed
            WindUI:Notify({ Title = "Velocidade", Content = "Definido para: " .. speed, Duration = 2 })
        end
    end
})

Tabs.GeneralTab:Toggle({
    Title = "Ativar speed",
    Value = false,
    Callback = function(state)
        translateAccelEnabled = state
        if translateConnection then
            translateConnection:Disconnect()
            translateConnection = nil
        end
        if state then
            translateConnection = RunService.Heartbeat:Connect(function()
                local char = LocalPlayer.Character
                if not char then return end
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                if not humanoid or humanoid.Health <= 0 then return end
                if humanoid.MoveDirection.Magnitude > 0 then
                    local moveDirection = humanoid.MoveDirection
                    local acceleration = moveDirection * translateSpeed / 30
                    char:TranslateBy(acceleration)
                end
            end)
            WindUI:Notify({ Title = "Speed", Content = "Ativado", Duration = 2 })
        else
            WindUI:Notify({ Title = "Speed", Content = "Desativado", Duration = 2 })
        end
    end
})

Tabs.GeneralTab:Divider()

Tabs.GeneralTab:Button({
    Title = "Voar (atravessar paredes)",
    Icon = "wind",
    Callback = function()
        WindUI:Notify({ Title = "Voo", Content = "Carregando script de voo...", Duration = 3 })
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/s1050031-debug/jsaj/refs/heads/main/F"))()
        end)
    end
})

Tabs.GeneralTab:Button({
    Title = "Remover lava",
    Icon = "flame",
    Callback = function()
        WindUI:Notify({ Title = "Lava", Content = "Removendo lava...", Duration = 3 })
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/mknhghv/67/refs/heads/main/lava.txt"))()
        end)
    end
})

Tabs.GeneralTab:Button({
    Title = "Remover neblina",
    Icon = "fog",
    Callback = function()
        WindUI:Notify({ Title = "Neblina", Content = "Removendo neblina...", Duration = 3 })
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/mknhghv/67/refs/heads/main/fog.txt"))()
        end)
    end
})

Tabs.GeneralTab:Button({
    Title = "Diminuir gráficos",
    Icon = "fps boost",
    Callback = function()
        WindUI:Notify({ Title = "Gráficos", Content = "Diminuindo gráficos...", Duration = 3 })
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/mknhghv/67/refs/heads/main/fps%20boost.txt"))()
        end)
    end
})